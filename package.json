{
  "name": "DNACaaP",
  "homepage": "https://wxparr.github.io/github.io",  
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "gh-pages": "1.0.0",
    "node-sass": "4.5.3",
    "npm-run-all": "4.1.1",
    "prop-types": "15.5.10"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "yarn build",
    "deploy": "gh-pages -b master -d build",
    "eject": "react-scripts eject",
    "clean": "rm -rf node_modules && yarn cache clean && yarn"
  }
}